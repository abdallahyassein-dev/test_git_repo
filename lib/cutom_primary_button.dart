import 'package:flutter/material.dart';

class CustomPrimayButton extends StatelessWidget {
  const CustomPrimayButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text("data"));
  }
}
